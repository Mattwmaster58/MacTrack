export const API_SUFFIX = "/api";

const ApiEndpoints = {
  search: `${API_SUFFIX}/search`,
  signIn: `${API_SUFFIX}/sign-in`,
  register: `${API_SUFFIX}/register`,
  filter: `${API_SUFFIX}/filter`,
};

export { ApiEndpoints };
